#include <string>
#include <vector>

#pragma once
namespace vsite {

	class Tokenizer
	{
	using namespace std;
	public:
		vector<string> tokenize(const )
	private:
		Tokenizer();
		~Tokenizer();
	};
}